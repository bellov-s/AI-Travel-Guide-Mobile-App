"use client";
export default function MapClient() {
  return (
    <div className="h-80 w-full rounded-[--radius] border bg-muted flex items-center justify-center text-sm text-muted-foreground">
      Map placeholder (add Mapbox/Leaflet later)
    </div>
  );
}
