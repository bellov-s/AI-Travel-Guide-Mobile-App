// components/MapClient.tsx
"use client";

export default function MapClient() {
  return (
    <div className="h-64 w-full rounded-[--radius] border bg-muted flex items-center justify-center text-sm text-muted-foreground">
      Map placeholder (connect Mapbox/Leaflet later)
    </div>
  );
}
